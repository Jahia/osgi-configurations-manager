- karafCommand: "log:log 'START - provisioning for osgi-configurations-manager'"
- karafCommand: "log:log 'No extra bundles or demo sites are required for these tests'"
# SUPPORT-646 / D5 — provision the TEST-ONLY scoped users for the authorization negatives
# (Cypress 08-authorization / 09-path-traversal, specs S22 / S48). See the script header and the
# Stage-5 report: the role/permission grant needs Stage-6 verification against the running instance.
- executeScript: "provision-scoped-users.groovy"
- karafCommand: "log:log 'END - provisioning for osgi-configurations-manager'"

